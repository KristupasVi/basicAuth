# Go-Auth

A simple command-line authentication system built with Go.

## Overview
This project provides a CLI tool for user registration and login. It uses the `bcrypt` library to securely hash passwords.

# Key features:
- Register a new user with a username and password.
- Login with existing credentials (3 attempts allowed).
- Passwords are hashed using `bcrypt` for security.
